#include<iostream>
#include<string>

/*
	int num=0;
	cin >> num;
	int count = 0;
	int temp = 0;
	int k = 0;
	for (int i = 1; i <= num; i++) {

		for (int j = 0; j < i; j++) {
			if (k == num)break;
			count += i;
			k++;
		}
	}
	cout << count;
	system("pause");
	
	
	using namespace std;
int main55() {
	int a, b, c;
	cin >> a >> b >> c;
	int min = 0;
	if (a > b) {
		min = b;
	}
	else {
		min = a;
	}
	int count = 0;
	for (int i = 0; i < c / min + 1; i++) {
		for (int j = 0; j < c / b + 1; j++) {
			if (i * a + b * j == c) {
				count++;
			}
			if (i * a + b * j > c) {
				break;
			}
		}
	}

	cout << count;
	system("pause");
	return 0;
}*/